using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EventManager : MonoBehaviour
{/*
    //This is put inside your class, but outside of Start or Update
[SerializeField] private static GameObject playerPrefab = null;

    private void Start()
    {
        InstantiatePlayer();
    }
    public static void InstantiatePlayer()
    {
        GameObject playerInstance = Instantiate(playerPrefab);
       playerInstance.transform.position = Vector3.zero; // just an example, set player position to 0,0,0
    }*/
}
